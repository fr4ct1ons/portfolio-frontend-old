export function isEmailValid(value)
{
    if(value.trim() === "")
    {
        return false;
    }

    return true;
}

export function isSubjectValid(value)
{
    if(value.trim() === "")
    {
        return false;
    }

    return true;
}

export function isMessageValid(value)
{
    if(value.trim() === "")
    {
        return false;
    }

    return true;
}